@using WebApp.Controllers
@using WebApp.Model.GenericMvc.ChildPage
@model Domain.DomainBase

@await Html.RenderPageHeaderAsync()

<div class="m-content">
    @await Html.PartialAsync("_DisplayModel")

    @await Html.PartialAsync("_ChildPage", ChildPageDescriptor.Create(nameof(IbansController), Model.Id, true))

    @await Html.PartialAsync("_ChildPage", ManyToManyChildPageDescriptor.Create(nameof(EntityProjectManyToManyRelationController), Model.Id, true))

    @await Html.RenderCrudButtonsAsync()
</div>