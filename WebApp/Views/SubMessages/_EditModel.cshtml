@using Domain.Message
@using WebApp.Model.Forms


@{
    var formDescriptor = FormBuilder<SubMessageDetails>.CreateNew(false, "Edit")
        .AddItem(x => x.Text)
        .AddItem(x => x.IsViewed)
        .AddItem(x => x.Number)
        .Build();
}

<div class="form-horizontal">
    @await Html.FormCrudAsync(formDescriptor)
</div>